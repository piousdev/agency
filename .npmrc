# Hoist React and related packages to prevent duplicate instances
# This is more controlled than shamefully-hoist=true
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*@sentry*

# Ensure consistent peer dependency resolution
strict-peer-dependencies=false