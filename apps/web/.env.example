# Next.js Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:8000

# Environment
NODE_ENV=development

# BetterAuth - Authentication & Session Management
# Note: The web app uses the API's BetterAuth instance via proxy routes
# No BETTER_AUTH_SECRET needed here - that's only for the API
# Authentication flows through NEXT_PUBLIC_API_URL configured above

# Database (if needed for direct queries from Next.js server components)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Analytics (PostHog) - Optional
# NEXT_PUBLIC_POSTHOG_KEY=phc_your_key_here
# NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# Sentry - Error Tracking & Monitoring
# Get your DSN from: https://sentry.io/settings/projects/your-project/keys/

# Server-side Sentry (for Server Components, API Routes, Server Actions)
SENTRY_DSN=https://xxxxx@oxxxxx.ingest.sentry.io/xxxxxx
SENTRY_ENVIRONMENT=development  # development, staging, or production
SENTRY_TRACES_SAMPLE_RATE=0.1   # 0.1 = 10%, 1.0 = 100%

# Client-side Sentry (for browser JavaScript - must be prefixed with NEXT_PUBLIC_)
NEXT_PUBLIC_SENTRY_DSN=https://xxxxx@oxxxxx.ingest.sentry.io/xxxxxx
NEXT_PUBLIC_SENTRY_ENVIRONMENT=development
NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE=0.1

# Sentry Project Configuration (for source map uploads)
# Get these from: https://sentry.io/settings/account/api/auth-tokens/
SENTRY_ORG=your-org-slug           # Your Sentry organization slug
SENTRY_PROJECT=your-project-slug   # Your Sentry project slug
SENTRY_AUTH_TOKEN=your_auth_token  # Auth token for uploading source maps (optional in dev)

# Development Mode
# SENTRY_SEND_IN_DEV=false  # Uncomment to send events in development (logs to console by default)
# SENTRY_DEBUG=false        # Enable Sentry SDK debug logging

# Development Mode Bypass
# ⚠️ WARNING: This bypasses security checks! Only use in local development
NEXT_PUBLIC_DEV_MODE_BYPASS=true